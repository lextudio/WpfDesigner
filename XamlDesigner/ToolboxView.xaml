<UserControl x:Class="ICSharpCode.XamlDesigner.ToolboxView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:Default="clr-namespace:ICSharpCode.XamlDesigner">
	<UserControl.ContextMenu>
		<ContextMenu>
			<MenuItem Header="Browse..." Click="BrowseForAssemblies_OnClick"></MenuItem>
		</ContextMenu>
	</UserControl.ContextMenu>

	<UserControl.Resources>
		<Style x:Key="ToolboxTileButtonStyle" TargetType="Button">
			<Setter Property="Margin" Value="0,0,10,10" />
			<Setter Property="Padding" Value="0" />
			<Setter Property="Background" Value="Transparent" />
			<Setter Property="BorderBrush" Value="Transparent" />
			<Setter Property="BorderThickness" Value="0" />
			<Setter Property="HorizontalContentAlignment" Value="Stretch" />
			<Setter Property="VerticalContentAlignment" Value="Stretch" />
			<Setter Property="Cursor" Value="Hand" />
			<Setter Property="Template">
				<Setter.Value>
					<ControlTemplate TargetType="Button">
						<Border x:Name="TileBorder"
						        Width="84"
						        Height="84"
						        CornerRadius="10"
						        Background="#FFF7F5F1"
						        BorderBrush="#FFE0D9CF"
						        BorderThickness="1">
							<StackPanel Margin="8" VerticalAlignment="Center">
								<Border Width="38"
								        Height="38"
								        CornerRadius="9"
								        Background="{Binding AccentColor}"
								        HorizontalAlignment="Center">
									<TextBlock Text="{Binding Glyph}"
									           Foreground="White"
									           FontWeight="SemiBold"
									           FontSize="12"
									           HorizontalAlignment="Center"
									           VerticalAlignment="Center"/>
								</Border>
								<TextBlock Margin="0,8,0,0"
								           Text="{Binding Name}"
								           TextAlignment="Center"
								           TextWrapping="Wrap"
								           MaxHeight="30"
								           FontSize="11"
								           FontWeight="SemiBold"
								           Foreground="#FF2C241D"/>
							</StackPanel>
						</Border>
						<ControlTemplate.Triggers>
							<Trigger Property="IsMouseOver" Value="True">
								<Setter TargetName="TileBorder" Property="Background" Value="#FFF1ECE3" />
								<Setter TargetName="TileBorder" Property="BorderBrush" Value="#FFB88955" />
							</Trigger>
							<Trigger Property="IsPressed" Value="True">
								<Setter TargetName="TileBorder" Property="Background" Value="#FFE8DFCF" />
							</Trigger>
						</ControlTemplate.Triggers>
					</ControlTemplate>
				</Setter.Value>
			</Setter>
		</Style>

		<DataTemplate x:Key="ToolboxControlTileTemplate" DataType="{x:Type Default:ControlNode}">
			<Button Style="{StaticResource ToolboxTileButtonStyle}"
			        Click="ControlTile_Click">
				<Button.ToolTip>
					<TextBlock Text="{Binding Type.FullName}" />
				</Button.ToolTip>
			</Button>
		</DataTemplate>

		<ItemsPanelTemplate x:Key="ToolboxWrapPanelTemplate">
			<WrapPanel />
		</ItemsPanelTemplate>
	</UserControl.Resources>

	<ScrollViewer VerticalScrollBarVisibility="Auto"
	              HorizontalScrollBarVisibility="Disabled"
	              BorderThickness="0">
		<ItemsControl ItemsSource="{Binding ToolboxGroups}">
			<ItemsControl.ItemTemplate>
				<DataTemplate DataType="{x:Type Default:ToolboxGroup}">
					<StackPanel Margin="0,0,0,18">
						<TextBlock Text="{Binding Name}"
						           Margin="4,0,0,8"
						           FontSize="13"
						           FontWeight="SemiBold"
						           Foreground="#FF3E3429" />
						<ItemsControl ItemsSource="{Binding Controls}"
						              ItemTemplate="{StaticResource ToolboxControlTileTemplate}"
						              ItemsPanel="{StaticResource ToolboxWrapPanelTemplate}" />
					</StackPanel>
				</DataTemplate>
			</ItemsControl.ItemTemplate>
		</ItemsControl>
	</ScrollViewer>
</UserControl>
